# Default values for test-manager.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
namespace: default
image:
  repository: jsroninfbk/echo
  pullPolicy: Always

service:
  type: NodePort
  port: 5000
  protocol: TCP

nodePort: 31000

image_repo: jsroninfbk/echo
image_tag: latest

# RR API port
api_port: 5000
