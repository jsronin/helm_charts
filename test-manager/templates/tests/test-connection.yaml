apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "test-manager.fullname" . }}-test-connection"
  labels:
    {{- include "test-manager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "test-manager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
